const message = 'hello!';

message.toLowerCase();

const user = {
    name: 'Daniel',
    age: 26,
};
// user.location;
/* ⬆️ returns undefined /and error added in typescript 
    Property 'location' does not exist on type '{ name: string; age: number; }'. */


